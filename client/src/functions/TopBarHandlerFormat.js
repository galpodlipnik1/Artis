export const handleTopBarFormat = (name) => {
  switch (name) {
    case 'Font':
      handleFont();
      break;
    case 'Align':
      handleAlign();
      break;
    case 'Borders':
      handleBorders();
      break;
  }
};

const handleFont = () => {};

const handleAlign = () => {};

const handleBorders = () => {};
