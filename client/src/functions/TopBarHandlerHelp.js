export const handleTopBarHelp = (name) => {
  switch (name) {
    case 'View Help':
      handleViewHelp();
      break;
    case 'About Paint':
      handleAboutArtis();
      break;
  }
};

const handleViewHelp = () => {};

const handleAboutArtis = () => {};
